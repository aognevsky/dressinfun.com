%h1= "Сообщение для #{@user.login}"

- form_for @message do |f|
  = f.error_messages
    
  %p
    = f.label 'subject'
    %br
    = f.text_field :subject
    
  %p
    = f.label 'body'
    %br
    = f.text_area :body, :cols => 48, :rows => 6
    
  = f.hidden_field :recipient, :value => @user.id

  %p
    = submit_tag 'Save'